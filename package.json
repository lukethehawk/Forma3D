{
  "name": "forma-3d",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "electron/main.cjs",
  "description": "Editor STL locale con flusso di lavoro ispirato a SketchUp",
  "scripts": {
    "prestart": "node scripts/assemble-main.cjs",
    "start": "npm run build && electron .",
    "predev": "node scripts/assemble-main.cjs",
    "dev": "vite --host 127.0.0.1",
    "prebuild": "node scripts/assemble-main.cjs",
    "build": "vite build",
    "test": "node --test"
  },
  "dependencies": {
    "three": "^0.184.0",
    "three-bvh-csg": "^0.0.18",
    "vite": "^8.0.16"
  },
  "devDependencies": {
    "electron": "^42.4.0"
  }
}
